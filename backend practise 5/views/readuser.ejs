<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
   <link rel="stylesheet" href="/backend practise 5/public/stylesheets/style.css">
</head>
<script src="https://unpkg.com/@tailwindcss/browser@4"></script>
<body>

    <nav class="bg-gray-800 p-4">
        <ul class="flex space-x-4">
            <li><a href="/" class="text-white">Back to home</a></li>
        </ul>
    </nav>
    <h1 class="text-3xl font-bold mb-4">All users</h1>

<div class="h-screen w-full text-white bg-zinc-900 flex  flex-wrap gap-4 ">

    <% if(users.length === 0) { %>
        <p class="text-2xl font-bold mt-4 text-gray-500">No users found</p>
    <%}else {%>
   
       <% users.forEach(function(user){%>

        <div class="bg-zinc-700 p-4 rounded-lg h-72 w-64">
            <div class="bg-zinc-500 h-32 w-full overflow-hidden mb-4">
                <img src="<%=user.img%>" alt="User Image" class="w-full h-full object-cover">
            </div>
            <h2 class="text-xl font-bold mb-2"><%=user.name%></h2>
            <p class="mb-4"><%=user.email%></p>
            <div class="flex justify-between">
                <button class="bg-blue-500 text-white px-4 py-2 rounded">Edit User</button>
                <a href="/delete/<%=user._id%>" class="bg-red-500 text-white px-4 py-2 rounded">Delete User</a>
            </div>
        </div>



      <% })%>
    <%}%>

        <!-- Repeat the above card for each user -->
   
</div>
    <script src="/javascripts/script.js"></script>
</body>
</html>